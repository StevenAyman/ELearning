name: Build & Test

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - 'feature/**'

env:
  DOTNET_VERSION: '9.x'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Setup .NET
        uses: actions/setup-dotnet@v5.2.0
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install dependencies
        run: dotnet restore ELearning.sln

      - name: Build
        run: dotnet build ELearning.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ELearning.sln --configuration Release --no-build
